extends layout.pug

block content
    .container.pb-3(id="chartContainer" )
    
        h1 Below are the polls
        
        ul
            each val, index in charts
                a(href= "https://secure-basin-74681.herokuapp.com/" + val._id ) 
                    li.mb-2.list-group-item= val.title 

